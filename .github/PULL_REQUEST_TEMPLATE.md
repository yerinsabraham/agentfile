## What stack is this?

<!-- Name and a one line description. Delete this comment. -->

## Checklist

- [ ] File is at `templates/stacks/<name>.md`, lowercase with hyphens
- [ ] It has `## what`, `## commands` and `## style`
- [ ] `## rerun` is present if the stack has a step with a trigger, and absent
      if it does not
- [ ] `rerun` says what triggers it, not just the command
- [ ] Commands are copied from a real project rather than remembered
- [ ] Two lines added to `templates/index.ts`
- [ ] No em dashes or en dashes

## Anything you were unsure about

<!-- Optional, and genuinely useful. If a question was hard to answer for your
     stack, say so. That is usually a gap in the question set rather than a
     gap in your knowledge, and it is how the question set improves. -->
